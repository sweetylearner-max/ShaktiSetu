<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.shaktisetu"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Device features -->
    <uses-feature
        android:name="android.hardware.telephony"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <!-- Storage: legacy support -->
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        tools:targetApi="23" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        tools:targetApi="23" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <queries>
        <package android:name="com.google.android.gms" />
        <package android:name="com.android.vending" />
        <package android:name="com.google.android.apps.maps" />
        <intent>
            <action android:name="com.google.android.gms.auth.service.START" />
        </intent>
        <intent>
            <action android:name="com.google.android.gms.location.service.START" />
        </intent>
        <intent>
            <action android:name="com.google.android.gms.auth.api.signin.SERVICE_CONFIG_UPDATED" />
        </intent>
    </queries>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/Theme.ShaktiSetu">
<!--        <activity-->
<!--            android:name=".TermsConditionsDialog"-->
<!--            android:exported="false"-->
<!--            android:hardwareAccelerated="true"/>-->
        <activity
            android:name=".EditProfileActivity"
            android:exported="false"
            android:hardwareAccelerated="true"/>
        <activity
            android:name=".NotificationActivity"
            android:exported="false"
            android:hardwareAccelerated="true"/>
        <activity
            android:name=".ProfileActivity"
            android:exported="false"
            android:hardwareAccelerated="true"/>

        <!-- FileProvider for camera/capture best practice -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.example.shaktisetu.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true" />

        <!-- Splash = LAUNCHER (start yahi se) -->
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:hardwareAccelerated="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

        </activity>

        <!-- Landing (login/welcome) -->
<!--        <activity-->
<!--            android:name=".LandingActivity"-->
<!--            android:exported="false" /> &lt;!&ndash; Main (home) &ndash;&gt;-->
        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:hardwareAccelerated="true"/>

        <!-- Settings -->
        <activity
            android:name=".SettingsActivity"
            android:exported="false"
            android:hardwareAccelerated="true"/>

        <!-- Fake Call -->
        <activity
            android:name=".FakeCallActivity"
            android:exported="true"
            android:hardwareAccelerated="true"/>

        <!-- Incoming Call (showOnLockScreen) -->
        <activity
            android:name=".IncomingCallActivity"
            android:exported="false"
            android:showOnLockScreen="true"
            android:theme="@style/Theme.AppCompat.NoActionBar"
            android:turnScreenOn="true"
            android:hardwareAccelerated="true"/>

        <!-- Contacts -->
        <activity
            android:name=".ContactsActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.ShaktiSetu"
            android:hardwareAccelerated="true"/>

        <!-- Add/Edit Contact -->
        <activity
            android:name=".AddEditContactActivity"
            android:exported="false"
            android:hardwareAccelerated="true"/>

        <!-- Evidence -->
        <activity
            android:name=".EvidenceActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.ShaktiSetu"
            android:hardwareAccelerated="true"/>

        <!-- SOS Alert -->
        <activity
            android:name=".SosAlertActivity"
            android:exported="false"
            android:hardwareAccelerated="true"/>

        <!-- Create PIN -->
        <activity
            android:name=".CreatePinActivity"
            android:exported="false"
            android:hardwareAccelerated="true"/>

        <!-- Dismiss PIN -->
        <activity
            android:name=".DismissPinActivity"
            android:exported="false"
            android:hardwareAccelerated="true"/>

        <!-- Sign In -->
        <activity
            android:name=".SignInActivity"
            android:exported="true"
            android:hardwareAccelerated="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data
                    android:scheme="https"
                    android:host="shaktisetu-18b97.firebaseapp.com"
                    android:pathPrefix="/__/auth/callback" />
            </intent-filter>
        </activity>

        <!-- Sign Up -->
        <activity
            android:name=".SignUpActivity"
            android:exported="false"
            android:hardwareAccelerated="true"/>

        <!-- Track Location Activity -->
        <activity
            android:name=".TrackLocationActivity"
            android:exported="true"
            android:hardwareAccelerated="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="shaktisetu"
                    android:host="track" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="android.max_aspect_ratio"
            android:value="2.1" />

        <!-- Enable high refresh rate -->
        <meta-data
            android:name="android.hardware.screen.orientation"
            android:value="portrait" />

        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyA8rirB71PKtvKFEVKhiIa0d63sRAWe0m4" />

        <service
            android:name=".MyFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
    </application>

</manifest>